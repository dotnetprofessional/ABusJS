/*
This class maybe necessary when adding additional transports

export class QueueEndpoint {
    host: string;
    name: string;

    public fullname(): string {
        return `${this.host}.${this.name}`;
    }
}
*/