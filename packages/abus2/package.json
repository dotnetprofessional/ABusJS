{
  "name": "abus2",
  "version": "0.6.0-alpha-1",
  "author": "Garry McGlennon",
  "description": "A service bus implementation for javascript.",
  "repository": {
    "url": ""
  },
  "main": "dist/src/index.js",
  "typings": "dist/src/index.d.ts",
  "ts:main": "src/index.ts",
  "keywords": [
    "abus",
    "service bus",
    "queue"
  ],
  "engines": {
    "node": "*"
  },
  "scripts": {
    "test": "mocha --colors --ui livedoc-mocha --reporter livedoc-mocha/livedoc-spec -O \"detailLevel=+summary+list\" --recursive dist/Features/",
    "test-spec": "mocha --colors --ui livedoc-mocha --reporter livedoc-mocha/livedoc-spec -O \"detailLevel=spec+summary+list\" --recursive dist/Features/",
    "release-web": "npm run dist && npm run compile-web-minify",
    "compile-web": "rimraf out && tsc -p tsconfig.web.json",
    "compile-web-minify": "uglifyjs out/abus.js --output out/abus.min.js --compress dead_code,drop_debugger,conditionals,booleans,loops,properties,drop_console --mangle --wrap abus",
    "release": "rimraf dist && tsc -b",
    "prepublish": "npm run release",
    "watch": "tsc --watch -p ."
  },
  "dependencies": {
    "diff": "^3.5.0"
  },
  "devDependencies": {
    "@types/chai": "^4.1.7",
    "@types/mocha": "^5.2.5",
    "@types/node": "^10.12.10",
    "lerna": "^3.6.0",
    "chai": "^4.2.0",
    "livedoc-mocha": "^0.4.0-beta-4",
    "mocha": "^5.2.0",
    "chai": "^4.2.0",
    "rimraf": "^2.6.2",
    "tslint": "^5.11.0",
    "typescript": "^3.2.2"
  }
}